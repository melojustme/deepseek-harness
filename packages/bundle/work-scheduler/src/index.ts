/**
 * @deepseek-ai/dsh-work-scheduler is a static profile patch carrier. The
 * profile composer resolves `cordis.patch.yml` through its manifest.
 * @module @deepseek-ai/dsh-work-scheduler
 */

export {}
